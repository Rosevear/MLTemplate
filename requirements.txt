#These are requriements for the overall project structure (e.g. documentation, environment variable libraries)
#Handled by the pip package manager (not Anaconda!)

# local package
-e .

# external requirements
click
Sphinx
coverage
flake8
python-dotenv>=0.5.1
